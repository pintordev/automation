name: findex keepalive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping findex
        run: |
          curl -s -o /dev/null -w "%{http_code}" \
            https://findex-by-code-slayers.up.railway.app/api/docs